{
  "name": "ambience-soundscape",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "deploy": "npm run build && wrangler pages deploy ."
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.0.0"
  }
}
